# v1.1.0 发布说明

本文件不属于运行时指令，执行出词任务时无需读取。

## 版本构成

- `SKILL.md` + `references/`（五个文件）：正式运行结构，frontmatter `name: prompt-assistant-online-v1-1-0`。
- `../提示词助手online v1.1.0.md`：同一内容的单文件形态，供人工通读与批注，`name` 与 `description` 与运行包一致。两者语义等价，差异只在指针形式（包内用「章节名」/《文件名》，单文件用 § 编号）。
- 与 v1.0.0 的差异只有一条新路由及其引用文件；`v1.0.0/` 原样保留，未做任何修改。
- 开发期版本与审计记录保留在 `../beta11/`、`../beta12/` 与对应单文件，不随正式版发布。

## v1.1.0 变更：新增 Qwen Image 2.1 文生图（T2I）路由

- **范围**：只纳入 Text-to-Image。Qwen 图像编辑（Edit）机制——画布跟随、`<image>` 编号、`ratio_follow`、原图像素保真、多图合并、换装换脸换背景扩图——一律不引入；编辑类请求按「出词与提问边界」简短说明当前 Qwen 路由只覆盖文生图。依据文件为 `../qwen-image2.1-system_prompt_t2i.txt`；`../qwen-image2.1-system_prompt_edit.txt` 仅用于边界辨别，两份原始 TXT 均未修改。
- **触发**：`Qwen Image 2.1`、`Qwen-Image-2.1`、`Qwen Image`、`通义万相 Image 2.1`，或上下文可可靠确定为 Qwen 图像生成模型的“Qwen”简称；未指定 Qwen 时其他路由行为与 v1.0.0 完全一致。
- **职责**：执行链不变，画面设计仍由「扩写调速器 → 统一视觉母版 → 视觉退火」完成；`Qwen Image 2.1 T2I Adapter` 只做表达编译——语言（英文）、叙述视角（观察者陈述）、信息展开程度、句子组织、JSON 结构、画幅字段。Adapter 不得改题。
- **优先级**：进入路由后，语言、视角、长度、比例字段与严格 JSON 交付协议优先于「默认对外输出」的在线模型代码块排版；用户硬要求、画面内容、扩写强度、视觉设计、单一动作时间点、第一视觉锚点、主光逻辑、正向可见事实、禁止擅加心理关系故事、视觉退火、Adapter 不重新设计画面，全部继续优先于 Qwen 规则。原始 Qwen 规则与整理版冲突时以 `references/Qwen-Image-2.1-T2I.md` 为准。
- **输出协议**：唯一交付物为单行严格 JSON `{"rewritten_prompt":"…","wh_ratio":"W:H"}`；无代码块、无 JSON 外文字、无第三字段、无 `ratio_follow`；比例只存在于 `wh_ratio`；`rewritten_prompt` 内无比例/像素/分辨率、无否定式、无空泛质量词，画面内文字逐字保留原语言并以转义直双引号包裹。
- **对原 Qwen 规则的修正**：其“输入短也发明整幅画面”的扩写职责移交扩写调速器；其鼓励频繁使用 `appears to be`/`likely`/二选一的犹豫register 被替换为“不主动制造不确定性，不得借不确定措辞回避母版已定决策”；其多段落分区写法因 JSON 单段无换行要求改为同段分区巡游。
- **落点文件**：`SKILL.md`（frontmatter、文件结构、执行链、默认对外输出例外、「Qwen Image 2.1 T2I 路由」新章节、最终自检 38–43）、`references/Adapter与交付.md`（「Qwen Image 2.1（T2I）」章节）、新文件 `references/Qwen-Image-2.1-T2I.md`、README 与单文件版同步。
- **验证状态**：发布时完成 `quick_validate.py` 校验、静态对账与 JSON 格式验证；真实 Qwen Image 2.1 出词、出图及跨模型复跑尚未完成，属于后续动态验证项。补充说明：中文 Windows 环境需以 UTF-8 模式运行校验器（`python -X utf8`，否则默认 GBK 解码会报 UnicodeDecodeError）；已完成的是规则与格式层面的静态验收（触发条款文本对账、构造样例的 JSON 解析与禁词扫描），不构成真实模型实跑，也不代表任何动态测试全部通过。

## 已完成的实跑验证（v1.0.0 时期，结论继续有效）

**1. 低强度扩写（输入为已写满机位、焦段、光向、构图位置、天气的成品级描述）**

结论：通过。用户已锁定的十项决定（主体、数量、服装、场景、天气、机位、景别、镜头、光向、构图位置）零改写；新增内容只落在空缺项（手部任务与武器状态、前中后景层次、面部可用的正面反射光、材质反光差异、色彩体系、画幅）。

需要留意的两点：

- 优先级判定正确。用户指定的"低机位"同时出现在「避免风格坍缩」的禁列套路里，实跑按「用户硬要求」高于自动补全处理，保留了低机位——没有出现风格系统反过来改用户决定的情况。
- 逆光下面部会失可见度，实跑补了一条由已有场景依据（雨夜地面反光）派生的正面反射光。这类补写属「补缺」定义范围内的正当行为，但它与"不重新导演用户画面"的界线值得继续观察：若后续出现借"提高执行稳定性"大改光位的情况，再收紧措辞。

**2. 导演／摄影师／片名内部检索键（输入"一个女人在深夜便利店门口，我想要杜可风那种感觉"）**

结论：通过。"杜可风"定位到 D01，取用偏心构图、前景反射、城市纵深、霓虹混合光、深黑阴影五项方法；条目自带的"潮湿表面"未被照搬——反射改由玻璃门、灯箱罩与塑料挡泥板承担，画面里没有雨后湿地，说明索引特征没有被当成固定套餐。人名未进入母版与成品，未带入角色、演员相貌、人物心理或剧情事件。

**3. 首次实跑（GPT，国风CLUB酒吧门口吸烟）**

约 15/20 项指定可见事实落地；平视加深焦、暖暗单色域、视线错开镜头、无否定词、无抽象风格词、无像素尺寸等约束生效。

## 运行测试观察项（非规则缺口，不改正文）

1. **湿反光街面**。模型自身的"夜色街面＝雨后反光"先验会在提示词毫无依据时补出湿地面。现有 §8、§9、§15、§25 已对无依据的湿润状态、湿地、雾气、反射与索引环境条件形成约束，本轮不新增重复规则。观察重点：调用 D01 等含潮湿表面特征的索引条目时，模型能否服从场景依据，而不是把该条目全部特征当作套餐取用。
2. **画面内招牌文字**。国风识别实际由模型自创的招牌文字承担（醉月／酒酣人间），文字未崩且可读性高。观察该现象是否稳定出现，以及它对构图与主题表达的干扰程度。

## 待议问题（未解决，等实际失败案例）

1. **服装形制的自由度归档**。「用户硬要求」允许基于时代、地域、身份与场合做知识驱动的服装补全，但「补充权限」未归档"用户未指定时的核心服装形制选择"属于哪一档。首次实跑中，吊带裙的领口由模型先验填成低胸，语域从黑帮片偏向写真。本轮不新增第四档，也不擅自归档，等更多失败案例再决定是否调整权限体系。
2. **GPT／Grok 的画面内文字条款**。Z-Image 与 Krea 2 路由有"目标文字原样保留并明确版面位置"，GPT／Grok 章节没有。登记为后续 Adapter 校准候选。
3. **裸模型对照**。同一输入在加载本 SKILL 与不加载时的产出差异仍未测量，登记为后续效果评估项，不作为发布门槛。
4. **正文示例已移除，回退条件**：若低强度实跑出现"把用户已定的画面改道"（换成其他时间、场景、机位等），把下面这一行原样放回「扩写调速器」核心原则之后：
   `例：“白发女剑客”为高强度；“白发女剑客站在雨夜竹林中央，黑色劲装，低机位全身，35mm，背后冷色逆光，人物位于右侧三分之一”为低强度补缺，不得改为夕阳、雪山、广角或中央对称的另一套画面。`
   三级强度判定的完整回归用例在 `../beta12/Beta12-扩写调速器对账表.md` 第 7 节。

## 已知限制

以上实跑由同一模型在加载 SKILL 后完成，能验证"规则是否被遵守、优先级是否被正确解决"，不能验证"换一个执行引擎是否同样遵守"，也不能替代真实出图的渲染验证。跨模型复跑仍需在目标模型上进行。
