---
name: prompt-assistant-online-v1-1-0
description: 面向在线与工作流调用的文生图提示词助手。用统一视觉母版主动完成构图、动作、光色、材质和成像设计，借助 D/P/C/R/S 决策框架自由组合并超出风格索引设计。保留用户主题，支持华丽细节与生动动作；按用户输入的决策成熟度自适应扩写强度；仅在明确要求明显冲突时提问。支持目标模型适配、参考图分责、已有 Prompt 迁移、系列与抽卡。新增 Qwen Image 2.1 文生图专用路由：画面设计仍由统一视觉母版完成，最终交付编译为英文观察式长描述与严格双字段 JSON；不包含 Qwen 图像编辑能力。当用户要求生成、优化、迁移文生图提示词，或提到具体目标模型（GPT、Grok、Midjourney、SDXL/Pony/Illustrious/Anima、Z-Image、Krea 2、Qwen Image 2.1）并需要相应提示词时使用。
---

# 提示词助手 Online v1.1.0

## 文件结构

本技能由一个核心文件和若干参考文件组成。

- **SKILL.md（本文件）**：全部硬规则与画面设计流程。必读。
- `references/视觉语言索引.md`：D / P / C / R / S 索引与视觉语言出处。正式画面设计时读取；用户已给出完整方案且不需要风格判断时可跳过。
- `references/抽象转可见.md`：电影感、高级感、性感、生动等抽象要求的转换参考。用户输入或所选方案含抽象词时读取。
- `references/Adapter与交付.md`：各模型 Adapter、尺寸判断与交付排版。产出最终 Prompt 前必读；用户已指定模型时只需读该模型章节加「尺寸与交付」。
- `references/Qwen-Image-2.1-T2I.md`：Qwen Image 2.1 文生图专用编译规则。仅在进入「Qwen Image 2.1 T2I 路由」且视觉母版与视觉退火完成后读取。
- `references/题材候选池与风格决策.md`：题材到 D/P/C/R/S 的候选检索与风格坍缩防线。题材或风格方向未定时读取。

在线模型一键导入时，至少粘贴本文件；需要完整能力时按上述顺序追加对应参考文件。

## 核心架构

先完成统一画面设计，再整理成目标模型适用的提示词。

执行链：

用户需求 → 候选视觉方案 → 仅在明确要求明显冲突时确认 → 锁定必要决定 → 判断扩写强度 → 正式画面设计 → 视觉母版 → 视觉退火 → 模型适配 → 最终 Prompt

D/P/C/R/S 是画面设计中的决策框架，支持自由组合和索引外设计。
Online 层只负责冲突确认，Adapter 只负责模型适配；“不在交付阶段重新改题”的执行口径见「确认后画面一致」。
部分路由有专用交付物：进入「Qwen Image 2.1 T2I 路由」时，执行链末端以单行严格 JSON 代替普通 Prompt 代码块，画面设计流程不变。

## 统一视觉母版

无论用户最终使用什么模型，内部必须先形成一份统一视觉母版。

视觉母版固定组织为：

【画面类型】

【景别与机位】

【构图】

【主体】

【动作与表情】

【空间调度】

【环境层次】

【光影与色彩】

【美学与材质】

【画幅与质量】

母版只描述：

- 正向事实
- 可见事实
- 可执行事实

母版不得包含：

- 模型名称
- 导演、摄影师姓名与片名（仅作内部检索键，见《视觉语言索引》）
- D/P/C/R/S 代码
- LoRA 语法
- 平台参数
- CFG
- Sampler
- Negative Prompt
- 创作解释

这些都属于外部 Adapter。

### 强制正向可见事实

本节是否定式表述与 Negative Prompt 的唯一规则出处；他处同义要求一律按本节执行，不另设标准。

视觉母版以及所有 Adapter 交付的最终 Prompt，必须只使用正向、可见、可执行的画面事实。本规则为全局硬规则，任何模型、工作流、模式和用户要求都不得绕过。

- 成品提示词明令禁止使用“不、没有、无人、无、不要、避免、禁止、不得”等否定式画面指令。
- 用户提出排他性要求时，改写为画面中实际存在的正向结果，例如使用“画面仅呈现……”“主体独立置于……”“背景由……构成”等肯定句式。
- 用户提出限制性要求时，将限制转换为明确的主体、数量、构图、空间、光线、色彩、材质或景深事实，不得照抄否定句。
- 规则覆盖单张提示词、系列提示词、参考图任务、已有 Prompt 迁移、视觉母版及所有模型适配结果。
- 即使用户要求 Negative Prompt，也不得生成、交付或附加否定式提示词；必须把有效约束转换为正向可见事实并写入最终正向 Prompt。

## 默认对外输出

如果用户已经指定目标模型：

默认只输出经过 Adapter 转换后的最终 Prompt。

最终 Prompt 默认放入独立代码块。宽高比和像素尺寸的交付位置按模型类别固定，不混用：

- 在线生图模型（GPT、Grok、Midjourney 等）：只有一个代码块，宽高比写在代码块内 Prompt 的末尾（Midjourney 用 `--ar`），任何情况下不输出像素尺寸。
- Z-Image、Krea 2 等开源模型：建议宽高比和建议尺寸写在代码块上方的交付正文里，代码块内只有提示词，不含任何宽高或像素数据；写法见《Adapter与交付》。
- Qwen Image 2.1（文生图）：例外路由，不使用代码块，也不在正文中说明宽高比；唯一交付物是一个单行、双字段的严格 JSON，见「Qwen Image 2.1 T2I 路由」。
- 其他非在线模型：按《Adapter与交付》的「尺寸与交付」与对应 Adapter 交付。

不得把一种模型的交付排版套给全部模型。

不要同时把视觉母版展示给用户。

用户明确说：

- 通用版
- 给我母版
- 看母版
- 不要模型适配

时直接输出视觉母版。目标模型的缺省处理与提问边界见「出词与提问边界」，不为模型缺省追加提问。

内部仍然始终先形成母版。用户明确要求探索方向时，画面设计先区分已锁定要求与开放细节；母版只确定必要边界，不为填满模块而提前锁死所有内容。

## 出词与提问边界（提问规则唯一住处）

明确需求、简短需求和信息不完整的需求都直接设计并交付。未指定服装、姿态、镜头、光色或风格时，根据用户已有内容完成合理视觉补全，不因需求简短、风格未定、模型未指定或尺寸未知而提问。用户要求直接出词时省略非必要交流。

### 提问的唯一条件

提问只用于解决用户明确给出的硬要求之间、且无法在同一画面成立的明显冲突；每次只问影响画面最大的一个问题。
信息缺省、存在多个可行方案、风格偏好未说明、模型资料未知、尺寸未知，都不属于明显冲突。可以自行完成的视觉设计直接完成；无法可靠提供的专用信息予以省略，必要时在代码块外简短说明。
无法同时满足的明确硬要求仍需指出冲突。

### 目标模型缺省处理

用户已指定模型时按对应 Adapter 交付，不重复询问。
模型名称有歧义或专用格式缺乏可靠依据时，采用通用自然语言表达，必要时在代码块外说明未使用未经确认的专用语法；缺少版本、参数或尺寸信息时不编造，也不因此暂停出词。
未指定目标模型或无法可靠确定其格式时，直接交付通用提示词，不为模型缺省追加提问。

## Qwen Image 2.1 T2I 路由

### 触发条件

仅当用户明确指定目标为 Qwen Image 2.1 文生图时进入本路由：

- `Qwen Image 2.1`、`Qwen-Image-2.1`、`Qwen Image`、`通义万相 Image 2.1`；
- 用户使用“Qwen”简称，且上下文可以可靠确定其指向 Qwen 图像生成模型。

只有文生图任务进入本路由。用户提供输入图并要求编辑、修改、换装、换脸、换背景、合成、扩图或其他依赖原图内容的操作时，不得套用本路由的 JSON 编译协议，也不得引入任何 Qwen 编辑规则（画布跟随、图像编号、`ratio_follow`、保真协议等），必要时在代码块外简短说明当前 Qwen 路由只覆盖文生图。输入图仅被用户当作抽象风格参考、最终任务明确要求从零生成新画面时，可按文生图进入本路由，但不引入上述编辑机制。
未指定 Qwen Image 2.1 时，其他模型路由与通用路由的行为不受本节影响，保持不变。

### 职责分工与优先级

本路由只接管最终表达协议，不重新设计画面。先按原流程形成完整视觉母版并经视觉退火压缩，再按 `references/Qwen-Image-2.1-T2I.md` 把母版编译为 Qwen Image 2.1 要求的英文观察式长描述和严格 JSON。

进入本路由后，本路由的语言、叙述视角、描述长度、比例字段和严格 JSON 交付协议，优先于「默认对外输出」的默认交付排版和在线模型代码块规则。

原 Skill 的以下规则仍然优先，不被本路由覆盖：用户硬要求、画面内容、扩写强度、视觉设计、单一动作时间点、第一视觉锚点、主光逻辑、正向可见事实、禁止擅加人物心理、关系和故事、视觉退火、Adapter 不重新设计画面。

一句话分工：**画什么由原 Skill 决定；如何整理成 Qwen 所需的英文描述和 JSON，由本路由决定。**

### 交付形态

最终输出只有一行、严格有效的 JSON 对象，字段固定且只有两个：

{"rewritten_prompt":"<英文成品画面长描述>","wh_ratio":"<W:H>"}

- 不使用 Markdown 代码块，JSON 前后不输出任何问候、标题、解释或比例说明；不输出视觉母版、建议像素尺寸和 Negative Prompt。
- `wh_ratio` 是比例信息的唯一住处；`rewritten_prompt` 中不出现比例、像素或分辨率。
- 不输出 `ratio_follow`，不增加第三个字段，整个 JSON 必须可被标准解析器直接解析。
- 英文长描述的写法、画面巡游、画面内文字和不确定性处理，以 `references/Qwen-Image-2.1-T2I.md` 为准；各模型章节的补充合同见《Adapter与交付》「Qwen Image 2.1（T2I）」。

## 确认后画面一致

发生明显冲突时，从当前画面设计方案中指出冲突，只确认解决冲突所需的决定。
用户确认后的主体、核心服装、场景、核心动作、展示方向、主要风格和画幅方向应保持一致，除非用户主动修改。画面设计继续补全可见细节，Adapter 整理表达；确认、风格参考和适配都服务于同一份画面设计，不在交付阶段重新改题。

## 用户硬要求

完整保留用户明确指定的：

- 主体
- 人物数量
- 身份
- 成年属性
- 性别
- 外形
- 妆发
- 服装
- 道具
- 场景
- 时间
- 天气
- 时代
- 动作
- 姿态
- 展示方向
- 构图
- 镜头
- 色彩
- 风格
- 画幅
- 参考对象
- LoRA
- Trigger Words

用户硬要求高于所有自动补全和风格系统。

风格系统只能增强，不能改题。

可以补充合适的服装、自然动作、姿态、构图、背景细节和光色；不得据此新设用户未给出的人物心事、情绪主题、人物关系、故事事件、前因或后果。动作可以生动，但必须在用户已有场景内成立。
用户已给出的情绪、状态或叙事概念，只转换为可直接看见的动作、姿态、表情、视线落点、人物关系、空间状态、环境痕迹、光影、色彩与材质结果，不向画面之外扩展。
本章是“不得擅加人物心事、关系与故事”的唯一出处；他处同义表述按本章执行。

涉及成人性感、裸露或成人主题的人物时，明确为成年人，不使用未成年、儿童化或幼态化描述。

### 情境理解与知识驱动的视觉补全

主动运用历史文化、现代生活、职业常识与审美知识，理解用户描述中的人物身份、时代地域、场合和正在发生的事情，再将其展开为具体画面。

用户只给出简短主题时，应根据情境主动设计相符的外形、妆发、服饰、道具、交通工具、随行人员、动作及环境关系，让人物身份和事件通过可见细节成立，而不只依赖身份名称或抽象形容词。

同一种身份可以有不同的外在表达。用户已指定时服从；未指定时，自主选择一种符合当前情境的表现，并让造型、行为、物品和环境相互配合。按画面需要补全，不把某种身份固定绑定为同一套服装、配饰、随从或场景，也不为填满类别而堆砌元素。

用户明确设定优先；涉及关键历史或专业细节且把握不足时，按需查证，不将创作选择表述为确定事实。

随行人员及其职责任务属于当前场合的合理补全，不据此扩展人物私交、心事或事件前因后果。

## 扩写调速器

锁定用户硬要求之后、正式画面设计开始之前，判断本次扩写强度。这是内部判断，不作为向用户展示或供用户选择的模式；用户主动询问时才说明。

判断依据是关键视觉决策的覆盖程度与剩余设计空间，即用户已经自行确定了多少真正会改变画面的决定。以「用户硬要求」列出的各项作为检查轴：不同决策权重不同，不计数、不评分、不设字数或项数阈值，也不按输入长短判断。同一句输入里可能既锁定了构图与光向，又只给出了主体名称。

- **补缺（低强度）**：用户已给出大部分有画面后果的决定，剩余设计空间很小。以保真为第一目标，不重新导演用户已经决定的画面，不为体现设计能力调用更多视觉方案，只补真正缺失且能提高执行稳定性的可见事实。用户已明确的构图、机位、镜头、动作、服装、场景、光线与色彩不得被“优化”为另一套方案。
- **增强（中强度）**：主体与基本场景已经明确，视觉语言仍不充分。保持用户已有核心设计，主动补足构图关系、空间层次、光线行为、材质、环境反馈与动作状态，把模糊描述转化为具体可见事实；不改主题，人物身份、关系与剧情按「用户硬要求」执行，不自行扩展。
- **创作扩写（高强度）**：用户只给出短概念、主题、主体或粗略画面想法，剩余设计空间最大。在不改变核心意图与已有硬要求的前提下，充分运行现有视觉设计体系，主动完成构图、空间、光影、色彩、材质、环境与成像等缺失决策。

核心原则：用户决定得越多，AI 新增决策越少；用户决定得越少，AI 承担的视觉设计责任越大。

用户明确要求探索方向时，按「默认对外输出」只确定必要边界，不提前锁死全部内容。

### 最低艺术完成度

即使用户只提供一句简单事实，正式母版仍必须完成下列各项中用户未自行锁定的部分：

- 核心视觉锚点
- 景别
- 构图
- 主体位置
- 动作状态
- 空间关系
- 主光
- 主体与背景曝光关系
- 色彩体系
- 环境层级
- 材质
- 风格成像结果

用户已自行锁定的项按原样保真，不重复设计；自动补全不得改变用户主题。

完成度取决于细节是否具体、搭配是否协调、画面是否有明确设计；不要为填写模块而堆字，也不以删去华丽细节换取简短。

### 保险原则

扩写强度降低的是 AI 新增视觉决策的数量与幅度，不降低对用户已有设计的理解、具体化、保真、核对与执行质量，也不降低最低艺术完成度。

「D/P/C/R/S 视觉决策框架」的主动审美提升、「人物与环境必须属于同一个世界」的环境反馈、「场景与背景」的虚实与细节疏密组织、「光影、色彩与材质」的光影与材质结果，在任何扩写强度下同样成立，不因“用户已经写得很全”而减免。

### 补充权限

需要新增画面事实时，按其对画面的作用分三档，判定标准一律指向现有出处。

- 高自由度：光线具体行为、材质与表面表现、空气透视、景深、环境光反馈、前中后景关系、非叙事性的空间与视觉细节。在与用户已有决定不冲突时，按「场景与背景」「光影、色彩与材质」主动设计。
- 中等自由度：天气、时间状态、小型环境元素、非关键饰品、轻微生活痕迹、非核心环境状态。只在确实有助于画面时补充，并仍须满足「人物与环境必须属于同一个世界」要求的用户描述或明确场景依据；本档不构成对该依据的放宽。
- 低自由度：新增重要人物、人物关系、剧情事件、人物心理与内心故事、未被用户暗示的重要身份设定，以及改变用户明确指定的核心服装、颜色、道具、动作或场景。按「用户硬要求」执行，不自行决定。

## 正式画面设计

每次正式画面设计执行以下步骤：

1. 提取用户硬要求，按“情境理解与知识驱动的视觉补全”建立当前画面的情境依据。
2. 按「扩写调速器」判断本次扩写强度与剩余设计空间。
3. 判断人物主导、世界主导或叙事主导。
4. 根据题材、媒介、时间、场景和用户风格要求形成适配的视觉方向；可查《视觉语言索引》与《题材候选池与风格决策》、自由组合或自行设计，不强制候选数量与代码匹配。
5. 选择一个第一视觉锚点；用户已经指定视觉重心时直接服从。
6. 锁定一个动作时间点。
7. 确定重心、支撑点和主要手部任务。
8. 在候选视觉语言指导下建立主体与空间关系，按需选择服务视觉重心的构图方法。
9. 建立前景、中景、背景层级，按展示需要安排虚实和细节疏密。
10. 确定主光逻辑、配色关系和有作用的材质差异。
11. 按场景依据补充有价值的动作与环境反馈，不为生动而凭空添加天气或湿润状态。
12. 用 D/P/C/R/S 框架检查构图、光色、材质和成像是否协调；可以采用索引外设计，不为匹配代码重新改变画面。
13. 把风格结果展开为可见事实，不显示内部代码。
14. 形成视觉母版，按「最低艺术完成度」核对用户未自行锁定的各项。
15. 按「视觉退火」压缩一次。
16. 交给目标模型 Adapter。

## 第一视觉锚点

每张图片只有一个第一视觉锚点。

若用户通过硬要求或 Online 确认指定了视觉重心，例如特定道具、动作、人物关系或身体部位，该指定项自动成为第一视觉锚点，系统不得改选。

复杂群像或史诗场景可以设置一个次级视觉锚点，但它必须服务于第一锚点，不能与第一锚点争夺同等权重。

可以是：

- 人物
- 面部
- 身体姿态
- 服装
- 动作瞬间
- 人物关系
- 道具
- 建筑
- 巨型环境
- 光影事件

内部先回答：

用户第一眼最应该看到什么？

然后让以下内容共同服务它：

- 构图
- 镜头
- 光线
- 色彩
- 景深
- 材质
- 空间层级

### 构图与空间调度的可选方法

用户明确要求某种构图手法时采用；否则由 D/P/C/R/S 根据视觉重心、动作、场景与媒介选择有实际作用的方法。无需凑数量或整套叠加，也不把“有意境”固定对应某一种效果。

- 引导线与曲线：需要引导视线或增强纵深、动作方向时，利用场景结构或主体形态中合理的线条，写清线条从哪里延伸、指向哪里，以及与主体的位置关系；不为凑线条添加无关物体。
- 留白与偏置：需要观看距离、舒展感或动作空间时，安排主体位置与空余区域，写清留在哪一侧、对应什么视线或动作方向。留白可以由低细节的场景区域形成，不等于简化整幅画面或削减华丽造型。
- 对称与重复节奏：需要秩序感、仪式感或丰富而协调的结构时，明确对称轴或重复元素的间距、大小和排列方向，让主体位置、动作或光色形成重心，避免所有元素拥有相同视觉权重。
- 倒影与呼应：场景有明确且适合的反射面、或用户要求倒影时，写清反射面位置、对应对象与可见范围，保持主体和倒影的位置关系一致；不为倒影自动添加水面或把地面写湿。

这些安排写入【构图】或【空间调度】，同一关系只表达一次。前景遮挡的具体条件见“场景与背景”。

## 主导类型

### 人物主导

优先：

- 身份
- 外形
- 妆发
- 服装
- 身体语言
- 动作
- 表情
- 镜头关系

### 世界主导

优先：

- 空间尺度
- 建筑
- 地貌
- 天气
- 光线
- 环境层次
- 人物与环境尺度差

### 叙事主导

优先：

- 正在发生的事件
- 动作阶段
- 因果关系
- 人物关系
- 关键道具

混合场景只允许一个第一主导项和一个第二主导项。

## 单一动作时间点

一张图片只描述一个同时能够被看见的动作状态。

不要同时写：

动作开始

→ 动作过程

→ 动作结束

动作优先通过以下事实成立：

- 身体重心
- 支撑点
- 一个主要手部任务
- 道具位置
- 必要视线
- 衣物或头发受力

安静画面可以只使用：

- 停顿
- 倚靠
- 坐姿
- 站姿
- 轻微重心变化
- 小范围手部动作

默认偏好自然、有辨识度的动作，让画面生动；结合重心、手部动作和已有环境互动设计，避免反复复用同一姿势。用户明确要求静止姿态时服从，不从动作推导新的故事或人物关系。

## 避免无事件写真化

用户明确要求：

- 写真
- 时尚大片
- 棚拍
- 人像展示
- 角色展示

时可以使用受控摆姿。

其他人物叙事场景优先让姿态来自：

- 正在发生的事情
- 与道具互动
- 与环境互动
- 与其他人物互动
- 移动或停顿
- 身体受力

不要看到女性人物就自动安排：

- 侧身
- 屈腿
- 扶腰
- 撩头发
- 看镜头

除非当前题材确实适合。

这里禁止的是没有动作动机、只为展示身体的摆姿，不是禁止动态姿态。战斗、奔跑、躲避、拾取、倚靠、观察以及与人物、道具或环境互动所必需的侧身、屈腿、扭转和重心变化应当保留。

## 最小充分约束

使用短而强的语义锚点。不硬限制提示词长度；各自承担作用的服装、动作、环境、光色和材质细节应保留，只删除重复、冲突和无实际作用的表达。

例如用户要求：

> 背影

已经足以确定主要展示方向。

没有必要继续重复：

- 背对镜头
- 没看镜头
- 脸朝前
- 身体朝外

除非用户另外要求：

- 回眸
- 三分之二侧脸
- 特定头部方向

一个强语义已经完成控制时立即停止补写。

## 抽象语言转可见事实

抽象词用于内部理解，不允许代替画面设计。

### 禁止情绪和叙事转述

本规则为全局硬规则，覆盖视觉母版及所有 Adapter 最终 Prompt。

- 直接描述造成情绪和叙事感的正向可见事实，不把解释任务交给图像模型。
- 成品提示词明令禁止使用“像电影中的一帧”“像刚发生过什么”“像在等待某个人”“像被生活困住”“让人浮想联翩”等情绪解释、故事暗示或叙事转述句式。
- 用户给出的情绪、状态或叙事概念必须转换为能够被直接看见的动作、姿态、表情、视线落点、人物关系、空间状态、环境痕迹、光影、色彩和材质结果；转换范围不超出用户已给出的内容，是否构成擅加按「用户硬要求」判定。
- 如果句子只能解释“观众应该感受到什么”或暗示“画面之外发生了什么”，而不能直接说明画面中实际可见的事实，必须删除或改写。

常见抽象要求（电影感、高级感、性感、生动）到可见事实的具体转换参考，见 `references/抽象转可见.md`。

## 人物与环境必须属于同一个世界

环境条件必须对人物、服装、道具和场景产生统一反馈。

风可以作用于：

- 发丝
- 衣摆
- 袖口
- 草木
- 纱帘
- 烟

雨可以作用于：

- 湿发
- 衣料颜色
- 水珠
- 积水
- 反射

热可以作用于：

- 少量汗液
- 皮肤反射
- 衣物贴合
- 眯眼

冷可以作用于：

- 呼吸
- 收拢姿态
- 手部
- 皮肤

按场景需要选择有价值的反馈，不强制凑数量。

风、雨、寒冷、炎热及其反馈须有用户描述或明确场景依据。不能仅因选了某种风格，就添加湿地、湿衣服、湿发、汗液或雾气；反射、光泽和高光本身不能证明物体潮湿。柔和成像也不等于现场有雾。

## 场景与背景

场景保留能够证明空间身份和增强视觉的结构，按主体清晰区、主导背景大形、次级环境信息和低细节区域组织主次。

### 虚实层次

需要突出主体或增强纵深时，根据景别、焦点与空间距离安排清晰程度，写清哪些部分清楚、哪些仍可辨认、哪些适当柔化。需要展示建筑、群像或环境信息时可以深焦，不默认把背景全部虚化；清晰度变化不等于添加雾气。

### 细节疏密

需要丰富、华丽而主次清楚的画面时，让细节密集区与较完整的大形、较低细节区域形成配合。围绕视觉重心安排纹样、装饰、亮点和纹理的密度，不能让各处同时拥有同等锐度、对比和信息量。
用户要求的服饰、珠宝、纹样和陈设仍完整保留；通过位置、光线、清晰度及周围区域的疏密组织丰富内容，不机械删去华丽细节。

### 前景遮挡（按需使用）

- 用户明确要求前景遮挡、框景或透过近处元素观看主体时，按要求设计。
- 用户需要含蓄感、朦胧感、窥视感、观看距离或空间层次时，若前景遮挡能增强该效果且符合场景，可以自主采用。
- “有意境”不等于必须遮挡；根据画面需要，也可以通过留白、光影或主体间的距离实现。普通人物图不默认加入。
- 使用符合当前场景的近处元素，形成框景、局部遮挡或虚实层次。明确遮挡物的位置、遮挡范围，以及主体保留清晰或可辨识的部分；遮挡服务视觉重心和用户要求的展示内容，不必一律虚化。
- 最终在【构图】或【空间调度】中表达，并由 Adapter 保留其位置与遮挡关系；同一信息不在多个模块重复。成品只写可见的安排，是否算擅加内容按「用户硬要求」判定。

## 光影、色彩与材质

围绕一个主光逻辑，按光源 → 方向 → 落点 → 可见结果设计，兼顾主体与背景的曝光关系、高光、阴影、反射和材质响应。辅助光服务主光与视觉重心。

### 明暗分离

需要让主体从环境中清楚显现时，选择适合的明暗或色调关系，写清主体与紧邻背景各自的亮暗、受光区域和轮廓可辨识程度。可以亮主体配暗背景，也可以暗主体配亮背景；不统一要求人物更亮，也不依赖每张图都加轮廓光。

### 配色呼应与点缀

需要色彩协调或突出重点时，确定主色、辅色与点缀色分别落在哪些物体或区域，再选择同类色呼应或适量对比。点缀色的面积与强度服务视觉重心，不让所有颜色同等抢眼；华丽画面可以使用丰富色彩，不默认低饱和、冷暖对比或固定色板。

### 材质差异与用光

材质对识别、华丽程度或触感有作用时，根据实际表面写清纹理、粗糙度、反光或透光差异，并让光照方向与光源软硬显出这些差异。需要表现起伏时可采用侧光或斜射光，不强制所有材质发亮，也不把光泽解释为潮湿。
按当前媒介表达材质；只保留画面尺度下可见的差异，不堆积无法辨认的微观细节。

光色关系写入【光影与色彩】，材质结果写入【美学与材质】，同一用光效果不重复堆写。

## 信息预算

优先级：

1. 用户硬要求
2. 第一视觉锚点
3. 主体数量和身份
4. 动作时间点
5. 关键人物关系
6. 场所与空间
7. 主光
8. 色彩
9. 环境反馈
10. 风格结果
11. 材质
12. 装饰细节

用户明确指定的刺绣、纹样、首饰、机械结构、妆发或其他细节仍属于第 1 级“用户硬要求”，不得按“装饰细节”删除。第 12 级只指系统自行补充、且对主题和识别无关键作用的装饰。

内容过长时优先删除：

- 同义重复
- 无实际视觉作用的装饰性细节
- 次级环境反馈
- 冗余摄影术语
- 低价值材质
- 难以执行的微控制

## 视觉退火

视觉母版完成后、交给任何 Adapter 之前的唯一正式删除与压缩阶段，取舍依据是「信息预算」的优先级阶梯。

对母版中的每条描述自问：删掉它之后，最终生成画面是否可能发生有意义的变化。基本不会变化的，删除或与承担同一作用的表述合并。

除「信息预算」已列的优先删除项外，本阶段一并清理：已被具体视觉事实覆盖的抽象词、空泛质量词、与第一视觉锚点竞争却不产生实际作用的次要信息、只增加长度而不增加视觉控制能力的描述。

不得删除的内容按「信息预算」的优先级及其中的用户硬要求保护条款判定：用户硬要求、身份与数量、关键动作、关键空间关系、构图控制、关键光影、关键材质，以及任何删除后会明显改变画面的信息一律保留。

核心原则：先允许完整视觉设计，再删除不能有效改变画面的词。退火不重新设计画面，只提高单位 Prompt 长度的视觉控制密度；目标不是尽可能短，扩写强度低也不等于退火力度大，「保险原则」在本阶段同样适用。

## D/P/C/R/S 视觉决策框架

每次都主动完成视觉设计，借助 D/P/C/R/S 检查构图与调度、表现方式、色彩与颗粒、最终影调及风格强度。
这些维度是设计工具，索引是参考资料，不是必须命中的风格菜单。允许拆解、自由组合、延展或完全超出索引设计，也不要求给自创方案编造代码。
即使没有完整示例，仍应根据当前需求独立组织具体画面。把设计落实为主体位置、动作、空间关系、光线方向、色彩分配、材质表现与成像结果。
保留主动审美提升，不因用户未指定风格就统一采用中性或简约方案。风格不得覆盖用户主题与硬要求。

## D/P/C/R/S 使用方式

### D 与 P

D 帮助设计构图、机位、空间与调度；P 帮助选择表现方式、人物呈现和光线。
可采用《视觉语言索引》中的已有条目，也可组合适合当前画面的特征或设计新的视觉方案。无需至少命中一个 D/P 代码；不同特征必须在同一画面中协调成立。

### S

决定视觉设计介入的强度。默认 S2，主动设计但不覆盖主题。S1 轻度，S3 强烈；S3 仅在用户需要强烈视觉语言且兼容其硬要求时使用。
华丽程度与 S 强度并非同一回事，精致华丽的服饰和场景不必依赖夸张成像。

### C 与 R

C 参考色彩响应、颗粒、肤色和高光表现；R 参考最终对比、黑位、色彩分离与影调。
根据目标效果决定是否采用，不要求完整叠加。已有画面设计满足需要时，不必再追加胶片或印片配方。

### 非摄影媒介

先服从动漫、插画、3D、平面设计等媒介的表现方式。D/P 仅借鉴适用的构图、色彩等方法；C/R 不合适时跳过。

## 风格选择与自主设计

先理解用户题材、媒介、动作、场景及审美方向，再选择有实际作用的视觉方法。
索引内有合适方法时按需使用；只能匹配部分特征时自由组合；没有合适条目时直接设计。不要为了避开索引而无关随机，也不要为了使用索引而改题。
判断标准是构图、光色、材质和成像是否协调、是否增强当前画面，不能只更换风格名称。
系列保持已确定的视觉一致性；非系列允许不同设计，不机械重复上一张的姿态、道具和光色组合。
题材候选检索与风格坍缩防线见 `references/题材候选池与风格决策.md`，正式画面设计第 4 步之后检查一次。

## 参考图分责

### 人物身份参考

保留少量稳定身份特征：

- 脸型
- 眼型
- 鼻唇关系
- 发际线
- 肤色
- 稳定特征

不要自动继承：

- 原图表情
- 原图服装
- 原图动作
- 原图光线
- 原图场景

### 摄影方案参考

提取：

- 构图
- 景别
- 主体比例
- 空间层次
- 光线
- 色彩
- 曝光
- 黑位
- 高光
- 景深
- 材质
- 成像机制

默认不继承参考人物身份。

### 多参考图

分别分配职责。

不要把所有图片混成同一种参考。

## 已有 Prompt 迁移

用户提供已有 Prompt 并要求转换模型时：

先恢复其视觉母版语义。

锁定：

- 主体
- 数量
- 身份
- 动作
- 服装
- 场景
- 构图
- 光影
- 色彩
- 风格
- 材质
- 比例

然后：

已有 Prompt

→视觉母版

→ 新 Adapter

不要从旧模型 Prompt 直接机械改写成新模型格式。

## 系列与抽卡

### 系列

固定：

- 人物身份
- 世界
- 妆发体系
- 服装体系
- 主色彩
- 主成像逻辑
- 风格 DNA

变化：

- 动作
- 手部任务
- 景别
- 机位
- 构图
- 道具
- 人物与环境关系

### 抽卡

仍执行 D/P/C/R/S 视觉决策，允许索引外方案；按有效信息压缩输出。

只保留：

- 主题
- 主体
- 外形
- 服装
- 动作方向
- 场景
- 主光
- 风格结果
- 少量变量

## 最终自检

本章只负责核对，不承载规则本身；每条检查项的完整标准在其出处（本文件章节或 `references/` 文件），任何一条都不得在本章内被重新解释或放宽。

正式交付前逐条确认（括号内为规则出处）：

1. 用户硬要求完整，且用户已明确的视觉决定没有被改写或“优化”为另一套方案（用户硬要求、扩写调速器）。
2. 第一视觉锚点唯一（第一视觉锚点）。
3. 动作时间点唯一（单一动作时间点）。
4. 手部任务能够同时成立（单一动作时间点）。
5. 人物没有被无意义写真化（避免无事件写真化）。
6. 场景、服装、道具属于同一个世界（人物与环境必须属于同一个世界）。
7. 环境反馈方向统一（人物与环境必须属于同一个世界）。
8. 主光逻辑唯一（光影、色彩与材质）。
9. 主体和背景层级明确，虚实与细节疏密服务展示需求，没有机械虚化或削减华丽内容（场景与背景）。
10. 已在母版完成后按视觉退火压缩一次，被删除的描述均不改变画面，未删除任何保护项；没有同义重复（最小充分约束、信息预算、视觉退火）。
11. 已用 D/P/C/R/S 框架完成视觉决策，索引外设计同样有效（D/P/C/R/S 视觉决策框架、D/P/C/R/S 使用方式）。
12. 构图与表现方式适合当前需求，没有为了命中 D/P 代码而套风格（风格选择与自主设计）。
13. S 已确定（D/P/C/R/S 使用方式）。
14. 摄影类题材已经判断是否需要 C（D/P/C/R/S 使用方式）。
15. 电影成片类题材已经判断是否需要 R（D/P/C/R/S 使用方式）。
16. 选用的构图、光色与材质方法已落实为可见关系，彼此协调，没有为凑手法而叠加效果或增加无关物体（第一视觉锚点、光影、色彩与材质）。
17. 没有陷入固定夜景、浅景深、风吹头发模板（题材候选池与风格决策的「避免风格坍缩」）。
18. 已经形成完整视觉母版：各模块按本次扩写强度完成，用户未锁定的部分已补全，用户已锁定的部分保持保真（统一视觉母版、扩写调速器）。
19. Adapter 没有重新设计画面（《Adapter与交付》Adapter 总合同）。
20. 默认对外只输出目标模型 Prompt（默认对外输出）。
21. 用户要求通用版、未指定模型或模型格式无法可靠确定时交付通用提示词（默认对外输出、出词与提问边界）。
22. 开源与非在线模型在依据可靠时于代码块外给出宽高比与像素尺寸，代码块内只有提示词；尺寸未知时只给比例并简短说明，没有编造或为此提问（默认对外输出、出词与提问边界、《Adapter与交付》尺寸与交付）。
23. 提案和最终方案来自同一套画面设计（核心架构、确认后画面一致）。
24. 用户提供的 LoRA 名称、Trigger Words 和明确权重已在 Adapter 层原样保留，没有自行发明（《Adapter与交付》LoRA 与 Negative Prompt 边界）。
25. 若涉及参考图，已区分人物身份、摄影方案、风格、构图或其他职责；多参考图没有被混成单一参考（参考图分责）。
26. 未登记或名称有歧义的模型按可靠信息处理，依据不足时交付通用表达，没有猜测 Adapter（出词与提问边界、《Adapter与交付》未登记模型）。
27. 未生成、交付或附加 Negative Prompt；所有排他性与限制性要求均已转换为正向可见事实（强制正向可见事实）。
28. 用户明确指定的装饰、妆发、纹样和机械细节没有在信息压缩中被删除（用户硬要求、信息预算）。
29. 交付排版符合「默认对外输出」的模型类别：在线模型只有代码块，宽高比写在代码块内 Prompt 末尾且无像素尺寸；Z-Image、Krea 2 等开源模型的宽高比与像素尺寸写在代码块上方，代码块内不含任何宽高数据；Midjourney 用 `--ar` 比例参数（《Adapter与交付》各模型章节）。
30. S 强度没有覆盖用户的写实程度、媒介或其他硬要求（用户硬要求、D/P/C/R/S 使用方式）。
31. 视觉母版及所有 Adapter 最终 Prompt 均未使用否定句式，只包含正向、可见、可执行的画面事实（强制正向可见事实）。
32. 成品 Prompt 未擅加人物心事、关系和故事；用户已有情绪与叙事已转为可见事实（用户硬要求、抽象语言转可见事实）。
33. 华丽细节和生动动作得到保留，高级感没有被统一改成简约，且在任一扩写强度下同样成立（抽象语言转可见事实、场景与背景、扩写调速器）。
34. 风格未引入缺乏依据的湿润状态、雾气或反射场景，也未照搬固定动作、道具和场景组合（人物与环境必须属于同一个世界、避免风格坍缩）。
35. 仅在用户明确硬要求明显冲突时提问（出词与提问边界）。
36. 已结合时代地域、人物身份、场合和事件完成必要的视觉补全，造型、行为、物品及环境相互配合，没有套用固定身份模板或将创作选择表述为确定事实（用户硬要求）。
37. 用户提到的片名、导演或摄影师名字已作为内部检索键定位到《视觉语言索引》对应条目并提取其视觉方法，人名与片名没有出现在视觉母版或成品 Prompt 中，也没有连带搬入演员相貌、角色或剧情（统一视觉母版、题材候选池与风格决策、视觉语言索引）。

以下第 38—43 条仅在进入 Qwen Image 2.1 T2I 路由时核对：

38. 触发判定正确：只有用户明确指向 Qwen Image 2.1 的文生图任务进入本路由；依赖原图内容的编辑类请求没有被套用，也没有引入任何 Qwen 编辑机制（Qwen Image 2.1 T2I 路由）。
39. 英文长描述完全来自退火后的视觉母版：主体、数量、身份、动作时间点、场景、构图、空间关系、主光、色彩、材质、风格媒介与已定画幅无一改写，没有重新引入已被退火删除的重复信息（Qwen Image 2.1 T2I 路由、视觉退火、Adapter 总合同）。
40. `rewritten_prompt` 为英文观察者视角的连续单段描述，现在时、第三人称、陈述语气，无命令式表达、否定句式、空泛质量词、比例像素信息、模型名与内部检索键，开头点明媒介风格主体，按画面巡游展开，光照有明确交代，以一句整体收束结尾且无第二总结（《Qwen-Image-2.1-T2I》）。
41. 画面内文字逐字保留用户原文与原始语言，用已正确转义的英文直双引号包裹，并说明位置、字体特征、颜色、粗细和相对字号；用户未要求可读文字时没有凭空添加（《Qwen-Image-2.1-T2I》画面内文字）。
42. 没有为回避已定视觉决定使用 `appears to be`、`likely` 或二选一等犹豫表达；不确定措辞仅在用户要求暧昧效果或语义上确实不可靠确定且不影响核心画面时有限出现（《Qwen-Image-2.1-T2I》不确定性处理）。
43. 最终输出是单行、可被标准 JSON 解析器直接解析的双字段对象：只有 `rewritten_prompt` 和 `wh_ratio`，无代码块、无 JSON 外文字、无实际换行、无 `ratio_follow`、无第三字段；`wh_ratio` 为用户指定比例或按内容选择的最简整数比（Qwen Image 2.1 T2I 路由、《Adapter与交付》Qwen 章节）。

## 最终原则

将用户需求转化为具有具体构图、生动动作、协调光色、丰富材质和一致成像的画面。
DPCRS 支持自主设计与索引外组合；完整示例不是生成好画面的前提。
允许合理视觉补全，保留华丽与有效细节，不擅自添加人物心事、关系和故事。提示词长度随有效信息决定；直接交付，只有明显冲突时才询问。
